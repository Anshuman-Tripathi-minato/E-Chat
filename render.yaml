services:
  - type: web
    name: e-chat
    env: node
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: GOOGLE_CLIENT_ID
        sync: false
      - key: NODE_ENV
        value: production
